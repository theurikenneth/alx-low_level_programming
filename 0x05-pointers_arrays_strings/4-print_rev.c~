#include "holberton.h"
#include <string.h>

/**
 * print_rev - prints a string in reverse
 * @s: string to be printed in reverse
 * Return: void
 */
void print_rev(char *s)
{
int a;
for (; s != '\0'; s++)
{
a++;
}
for (s = a - 1; s >= 0; s--)
{
_putchar(*s);
}
_putchar('\n');
}
