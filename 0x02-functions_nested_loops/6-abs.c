#include "holberton.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of int
 *
 * Return: returns n if positive and -n if negative
 */
int _abs(int r)
{
if (r >= 0)
return (n);
else
return (-n);
}
