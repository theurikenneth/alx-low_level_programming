#include "holberton.h"
#include <stdio.h>

/**
 * add - adds two integers
 * @m: the first integer
 * @n: the second integer
 *
 * Return: returns the sum of the two integers
 */
int add(int m, int n)
{
return (m + n);
}
