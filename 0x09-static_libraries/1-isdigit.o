#include "holberton.h"

/**
 * _isdigit - checks whether it is a digit
 * @c: tested character
 * Return: 1 if uppercase, else 0
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
return (1);
else
return (0);
}