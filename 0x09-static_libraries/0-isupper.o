#include "holberton.h"
#include "libholberton.a"

/**
 * _isupper - checks whether it is upper case
 * @c: tested character
 * Return: 1 if uppercase, else 0
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}