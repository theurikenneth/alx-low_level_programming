#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int num;

for (num = 0; num <= 19; num++)
{
  putchar((num % 20) + '0');
  if (num == 19)
    continue;
  putchar(',');
  putchar('');
}
 putchar('\n');

return (0);
}
